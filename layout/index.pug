extends _partial/layout.pug

block content
  //- include _partial/recent-posts.pug
  include _macro/post.pug
  section#posts.posts-expand
    each post in page.posts.data
      +post_item(post, true)
  include _partial/pagination.pug
