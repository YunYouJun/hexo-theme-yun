@import './back-to-top';

if (hexo-config('engine_search.enable') || hexo-config('local_search.enable') || hexo-config('algolia_search.enable')) {
  @import './search';
}

if (hexo-config('comment.enable')) {
  @import './comment';
}

if (hexo-config('banner.cloud')) {
  @import './cloud';
}

if (hexo-config('banner.go_down.enable')) {
  @import './go-down';
}

if (hexo-config('say.enable')) {
  @import './say';
}

if (hexo-config('notice.enable')) {
  @import './notice';
}