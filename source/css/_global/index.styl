@import "reset.styl"

// body
  // background: radial-gradient(#e6e9f0, #fefefe)
  // background-attachment: fixed

body
  --mdc-theme-primary: $colors.primary
  --mdc-layout-grid-gutter-desktop: 2px
  --hts-theme-primary: $colors.primary