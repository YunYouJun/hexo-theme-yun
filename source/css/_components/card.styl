.hty {
  &-card {
    position: relative;
    display: flex;
    flex-direction: column;
    box-sizing: border-box;
    the-transition();

    &:hover {
      box-shadow: 0 0 1rem rgba(0, 0, 0, 0.1);
    }
  }
}
